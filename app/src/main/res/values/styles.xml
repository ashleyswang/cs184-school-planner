<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBaseTheme" parent="android:Theme.Material.Light">
        <item name="android:datePickerDialogTheme">@style/TermsDatePicker</item>
    </style>

    <style name="TermsDatePicker" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorAccent">@color/medGrey</item>
        <item name="android:textSize">@dimen/datepicker_header_size</item>
    </style>

</resources>